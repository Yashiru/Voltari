# Godot 4
.godot/
/android/
export_presets.cfg
*.translation

# Third-party addons are not vendored — installed locally and in CI.
/addons/gdUnit4/

# gdUnit4 test reports
/reports/

# Content build artefacts, generated from content/*.yaml. Never hand-edited.
/game/data/generated/

# Oracle vector generation tooling
node_modules/

# OS
.DS_Store
Thumbs.db

# Third-party species models, used to test gameplay until the real assets
# arrive. They must never enter this repository — the IP is 100% original.
# Installed by pogo_assets/_tools/pipeline.py, which refuses to write here
# unless git confirms this path is ignored.
/game/assets/species/
