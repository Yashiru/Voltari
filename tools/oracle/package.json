{
  "name": "voltari-oracle",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Oracle vector generation for Voltari. Development tooling only — never shipped, never read by the engine.",
  "scripts": {
    "probe": "node probe.mjs"
  },
  "dependencies": {
    "@pkmn/sim": "0.10.11"
  }
}
